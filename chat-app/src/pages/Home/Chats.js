import React from 'react';

function Chats(props) {
    // here we will take which username chats we need to render from props.user passed by Users Component
    return (
        <div>
            
        </div>
    );
}

export default Chats;